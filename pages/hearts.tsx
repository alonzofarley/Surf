import { GameBoard } from "@/components/hearts/gameboard";
import React from "react";

export default function Index(props: {}) {
  return (
    <React.StrictMode>
      <GameBoard />
    </React.StrictMode>
  );
}
